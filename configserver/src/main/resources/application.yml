spring:
  application:
    name: "configserver"
  profiles:
    # active: native
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yassineBenabda/config-repo.git
          default-label: main
          clone-on-start: true
        # native:
          # search-loactions : "classpath:/config"
          # search-locations: file:///C:/Users/benab/IdeaProjects/yassine-spring-cloud/config-repo

server:
  port: 9999
management:
  endpoints:
    web:
      exposure:
        include: '*'